Test cases for mdvic.

- Run tests via `tests/run.sh`
- Uses deterministic settings: `--no-color`, `MDVIC_NO_OSC8=1`, fixed `--width`.
- Compares stdout to golden `.out` and stderr to `.err` if present.

